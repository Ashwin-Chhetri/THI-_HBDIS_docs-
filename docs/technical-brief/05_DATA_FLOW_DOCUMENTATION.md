# Data Flow Documentation
## HMBIS Knowledge Common - Data Movement & Processing

**Document**: 05 of 08  
**Date**: November 6, 2025  
**Version**: 1.0

---

## Table of Contents

1. [Data Flow Overview](#1-data-flow-overview)
2. [Source to Storage Flow](#2-source-to-storage-flow)
3. [Storage to Visualization Flow](#3-storage-to-visualization-flow)
4. [User Interaction Flows](#4-user-interaction-flows)
5. [Data Update Cycles](#5-data-update-cycles)
6. [Layer Dependency Flow](#6-layer-dependency-flow)
7. [Export & Download Flow](#7-export--download-flow)

---

## 1. Data Flow Overview

### 1.1 High-Level Data Movement

```
┌───────────────────────────────────────────────────────────────┐
│                    DATA SOURCES                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │   GBIF   │  │  Local   │  │Regional  │  │   User   │     │
│  │   API    │  │   DB     │  │   APIs   │  │ Uploads  │     │
│  └─────┬────┘  └─────┬────┘  └─────┬────┘  └─────┬────┘     │
└────────┼─────────────┼─────────────┼─────────────┼───────────┘
         │             │             │             │
         └─────────────┴─────────────┴─────────────┘
                           │
                           ▼
        ┌─────────────────────────────────────────┐
        │      DATA INGESTION LAYER               │
        │  • API Clients                          │
        │  • File Parsers                         │
        │  • Data Adapters                        │
        └──────────────────┬──────────────────────┘
                           │
                           ▼
        ┌─────────────────────────────────────────┐
        │      VALIDATION & TRANSFORMATION        │
        │  • Schema Validation                    │
        │  • Quality Checks                       │
        │  • Format Conversion                    │
        │  • Taxonomy Harmonization               │
        └──────────────────┬──────────────────────┘
                           │
                           ▼
        ┌─────────────────────────────────────────┐
        │         DATA STORAGE LAYER              │
        │  ┌────────────┐  ┌─────────────┐       │
        │  │ IndexedDB  │  │  PostgreSQL │       │
        │  │ (Client)   │  │  (Server)   │       │
        │  └────────────┘  └─────────────┘       │
        └──────────────────┬──────────────────────┘
                           │
                           ▼
        ┌─────────────────────────────────────────┐
        │       STATE MANAGEMENT (Redux)          │
        │  • Layer State                          │
        │  • Dependency Graph                     │
        │  • UI State                             │
        └──────────────────┬──────────────────────┘
                           │
                           ▼
        ┌─────────────────────────────────────────┐
        │      VISUALIZATION LAYER                │
        │  • MapLibre GL JS                       │
        │  • React Components                     │
        │  • Chart Libraries                      │
        └─────────────────────────────────────────┘
```

### 1.2 Data Flow Characteristics

| Flow Type | Latency | Volume | Frequency | Caching |
|-----------|---------|--------|-----------|---------|
| **GBIF API Call** | 500ms-2s | 1-10K records | On-demand | 24hr TTL |
| **User Upload** | < 1s | 1-100K records | Ad-hoc | Permanent |
| **Layer Rendering** | < 500ms | Any | On state change | None |
| **Map Tiles** | 50-200ms | Per tile | On pan/zoom | Browser |
| **Search Query** | 100-300ms | 1-100 results | On keystroke | 1hr TTL |

---

## 2. Source to Storage Flow

### 2.1 GBIF Occurrence Data Flow

```
USER ACTION: "Search for Red Panda"
  ↓
┌─────────────────────────────────────────────┐
│ 1. SPECIES SEARCH                            │
│    Component: SpeciesSearch.jsx              │
│    Action: searchSpeciesRequest('red panda') │
└──────────────────┬──────────────────────────┘
                   │ dispatch(action)
                   ▼
┌─────────────────────────────────────────────┐
│ 2. REDUX SAGA INTERCEPT                      │
│    Saga: speciesSagas.js                     │
│    Function: handleSpeciesSearch()           │
└──────────────────┬──────────────────────────┘
                   │ yield call()
                   ▼
┌─────────────────────────────────────────────┐
│ 3. API CLIENT CALL                           │
│    Service: gbifService.js                   │
│    Method: searchSpecies('red panda')        │
│                                              │
│    Request:                                  │
│    GET https://api.gbif.org/v1/species/search│
│    ?q=red+panda&limit=20                     │
└──────────────────┬──────────────────────────┘
                   │ HTTPS
                   ▼
┌─────────────────────────────────────────────┐
│ 4. GBIF API RESPONSE                         │
│    Status: 200 OK                            │
│    Body: {                                   │
│      results: [                              │
│        {                                     │
│          key: 5218933,                       │
│          scientificName: "Ailurus fulgens",  │
│          kingdom: "Animalia",                │
│          ...                                 │
│        }                                     │
│      ]                                       │
│    }                                         │
└──────────────────┬──────────────────────────┘
                   │ return response
                   ▼
┌─────────────────────────────────────────────┐
│ 5. DATA TRANSFORMATION                       │
│    Transform to app format:                  │
│    {                                         │
│      taxonKey: 5218933,                      │
│      scientificName: "Ailurus fulgens",      │
│      commonName: "Red Panda",                │
│      rank: "SPECIES",                        │
│      ...                                     │
│    }                                         │
└──────────────────┬──────────────────────────┘
                   │ yield put()
                   ▼
┌─────────────────────────────────────────────┐
│ 6. UPDATE REDUX STATE                        │
│    Action: searchSpeciesSuccess(results)     │
│    State Update:                             │
│    state.species.searchResults = [...]       │
│    state.species.searchLoading = false       │
└──────────────────┬──────────────────────────┘
                   │ selector fires
                   ▼
┌─────────────────────────────────────────────┐
│ 7. COMPONENT RE-RENDER                       │
│    Component: SpeciesSearch.jsx              │
│    Display: List of species results          │
│    User sees: "Ailurus fulgens - Red Panda"  │
└─────────────────────────────────────────────┘
```

### 2.2 GBIF Occurrence Fetch Flow

```
USER ACTION: Click on "Ailurus fulgens"
  ↓
┌─────────────────────────────────────────────┐
│ 1. SELECT SPECIES                            │
│    Action: selectSpecies(species)            │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 2. FETCH OCCURRENCES SAGA                    │
│    Saga: handleSpeciesSelection()            │
│                                              │
│    Steps:                                    │
│    a) Get active region from state           │
│    b) Build query parameters                 │
│    c) Call GBIF occurrences API              │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 3. GBIF OCCURRENCE API CALL                  │
│    GET /occurrence/search?                   │
│      taxonKey=5218933                        │
│      &geometry=POLYGON((88 27, ...))         │
│      &hasCoordinate=true                     │
│      &limit=10000                            │
│                                              │
│    Response: 1,543 records                   │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 4. VALIDATE & TRANSFORM                      │
│    For each record:                          │
│    - Validate coordinates                    │
│    - Check date format                       │
│    - Harmonize taxonomy                      │
│    - Calculate quality score                 │
│                                              │
│    Validation Results:                       │
│    Valid: 1,543                              │
│    Invalid: 0                                │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 5. CONVERT TO GEOJSON                        │
│    {                                         │
│      type: "FeatureCollection",              │
│      features: [                             │
│        {                                     │
│          type: "Feature",                    │
│          geometry: {                         │
│            type: "Point",                    │
│            coordinates: [88.6065, 27.3389]   │
│          },                                  │
│          properties: { ... }                 │
│        },                                    │
│        ...                                   │
│      ]                                       │
│    }                                         │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 6. STORE IN INDEXEDDB                        │
│    Database: thi-knowledge-common            │
│    Store: datasets                           │
│    Key: gbif-5218933-sikkim                  │
│    Data: GeoJSON (45MB)                      │
│                                              │
│    Write time: ~2 seconds                    │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 7. UPDATE REDUX (METADATA ONLY)             │
│    state.species.occurrenceData = null       │
│    state.species.occurrenceCount = 1543      │
│                                              │
│    state.layers.byId['gbif-species'] = {     │
│      id: 'gbif-species',                     │
│      status: 'active',                       │
│      dataReference: 'indexeddb://...',       │
│      recordCount: 1543                       │
│    }                                         │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 8. TRIGGER LAYER RENDERING                   │
│    (See Section 3 for visualization flow)    │
└─────────────────────────────────────────────┘
```

### 2.3 User File Upload Flow

```
USER ACTION: Drag & drop GeoJSON file
  ↓
┌─────────────────────────────────────────────┐
│ 1. FILE DROP EVENT                           │
│    Component: FileUpload.jsx                 │
│    Event: onDrop(files)                      │
│    File: sikkim-boundary.geojson (450 KB)    │
└──────────────────┬──────────────────────────┘
                   │ dispatch(action)
                   ▼
┌─────────────────────────────────────────────┐
│ 2. FILE UPLOAD SAGA                          │
│    Saga: handleFileUpload()                  │
│                                              │
│    Progress Updates:                         │
│    - Parsing: 0%                             │
│    - Validating: 20%                         │
│    - Processing: 40%                         │
│    - Storing: 80%                            │
│    - Complete: 100%                          │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 3. PARSE FILE                                │
│    Service: fileParserService.js             │
│    Method: parseGeoJSON(file)                │
│                                              │
│    Steps:                                    │
│    a) Read file as text                      │
│    b) JSON.parse()                           │
│    c) Validate GeoJSON structure             │
│                                              │
│    Result:                                   │
│    {                                         │
│      type: "FeatureCollection",              │
│      features: [5 MultiPolygon features]     │
│    }                                         │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 4. VALIDATE GEOMETRY                         │
│    Checks:                                   │
│    ✓ Valid GeoJSON structure                 │
│    ✓ Coordinates in valid range              │
│    ✓ No self-intersections                   │
│    ✓ Clockwise/counter-clockwise winding     │
│                                              │
│    Validation: PASSED                        │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 5. DETECT LAYER TYPE                         │
│    Analyzer: detectLayerType()               │
│                                              │
│    Analysis:                                 │
│    - Geometry: Polygon/MultiPolygon          │
│    - Properties: region_name, type           │
│    - Decision: REGION boundary layer         │
│                                              │
│    Detected Type: REGION                     │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 6. CALCULATE METADATA                        │
│    Using Turf.js:                            │
│    - Bounding Box: [87.5, 27.0, 89.0, 28.5]  │
│    - Area: 7,096 km²                         │
│    - Centroid: [88.25, 27.75]                │
│    - Feature Count: 5                        │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 7. STORE IN INDEXEDDB                        │
│    Store: user-uploads                       │
│    Record: {                                 │
│      id: 'user-region-1699000000',           │
│      filename: 'sikkim-boundary.geojson',    │
│      type: 'REGION',                         │
│      geometry: {...},                        │
│      metadata: {...},                        │
│      uploadedAt: 1699000000000               │
│    }                                         │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 8. ADD TO LAYER SYSTEM                       │
│    dispatch(addLayer({                       │
│      id: 'user-region-1699000000',           │
│      type: 'REGION',                         │
│      category: 'BOUNDARIES',                 │
│      source: 'user-upload',                  │
│      dataReference: 'indexeddb://...',       │
│      metadata: {...}                         │
│    }))                                       │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 9. DEPENDENCY CASCADE                        │
│    Middleware detects new REGION layer       │
│    → Triggers refresh of dependent layers:   │
│      - gbif-species                          │
│      - local-species                         │
│      - ecosystem-data                        │
│                                              │
│    Each dependent layer re-fetches with      │
│    new region filter                         │
└─────────────────────────────────────────────┘
```

---

## 3. Storage to Visualization Flow

### 3.1 Layer Rendering Flow

```
TRIGGER: Layer added to Redux state
  ↓
┌─────────────────────────────────────────────┐
│ 1. REDUX STATE CHANGE                        │
│    state.layers.byId['gbif-species'] = {     │
│      id: 'gbif-species',                     │
│      status: 'active',                       │
│      dataReference: 'indexeddb://...',       │
│      category: 'BIODIVERSITY'                │
│    }                                         │
└──────────────────┬──────────────────────────┘
                   │ useEffect() triggers
                   ▼
┌─────────────────────────────────────────────┐
│ 2. COMPONENT EFFECT                          │
│    Component: MapContainer.jsx               │
│                                              │
│    useEffect(() => {                         │
│      if (newLayer) {                         │
│        layerManager.syncWithReduxState(...)  │
│      }                                       │
│    }, [layers])                              │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 3. ENHANCED LAYER MANAGER                    │
│    Method: syncWithReduxState()              │
│                                              │
│    Actions:                                  │
│    - Compare current map layers vs Redux     │
│    - Identify new layers to add              │
│    - Queue operations in batch               │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 4. LOAD DATA FROM INDEXEDDB                  │
│    const db = await openDB(...)              │
│    const data = await db.get(                │
│      'datasets',                             │
│      'gbif-5218933-sikkim'                   │
│    );                                        │
│                                              │
│    Retrieved: GeoJSON with 1,543 features    │
│    Load time: ~100ms                         │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 5. DETERMINE VISUALIZATION MODE              │
│    Check: state.species.visualizationMode    │
│                                              │
│    Modes:                                    │
│    - 'points': Individual markers            │
│    - 'heatmap': Density heatmap              │
│    - 'grid': Grid aggregation                │
│                                              │
│    Selected: 'heatmap'                       │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 6. ADD MAPLIBRE SOURCE                       │
│    map.addSource('gbif-species-source', {    │
│      type: 'geojson',                        │
│      data: geojsonData                       │
│    });                                       │
│                                              │
│    Processing: MapLibre parses GeoJSON       │
│    Time: ~200ms for 1,543 points             │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 7. ADD MAPLIBRE LAYER                        │
│    map.addLayer({                            │
│      id: 'gbif-species',                     │
│      type: 'heatmap',                        │
│      source: 'gbif-species-source',          │
│      paint: {                                │
│        'heatmap-weight': 1,                  │
│        'heatmap-intensity': 1,               │
│        'heatmap-color': [                    │
│          'interpolate',                      │
│          ['linear'],                         │
│          ['heatmap-density'],                │
│          0, 'rgba(0,0,255,0)',               │
│          0.5, 'rgb(0,255,0)',                │
│          1, 'rgb(255,0,0)'                   │
│        ],                                    │
│        'heatmap-radius': 30                  │
│      }                                       │
│    });                                       │
│                                              │
│    Rendering: WebGL shader compilation       │
│    Time: ~100ms                              │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 8. VISUAL UPDATE COMPLETE                    │
│    User sees: Heatmap on map                 │
│    Total time: ~400ms from state change      │
│                                              │
│    Legend updated                            │
│    Layer control updated                     │
│    Statistics panel updated                  │
└─────────────────────────────────────────────┘
```

### 3.2 Dynamic Layer Update Flow

```
USER ACTION: Change heatmap opacity to 0.5
  ↓
┌─────────────────────────────────────────────┐
│ 1. UI CONTROL CHANGE                         │
│    Component: LayerControls.jsx              │
│    Event: onOpacityChange(0.5)               │
└──────────────────┬──────────────────────────┘
                   │ dispatch(action)
                   ▼
┌─────────────────────────────────────────────┐
│ 2. UPDATE REDUX STATE                        │
│    Action: updateLayerOpacity({              │
│      layerId: 'gbif-species',                │
│      opacity: 0.5                            │
│    })                                        │
│                                              │
│    State:                                    │
│    state.layers.byId['gbif-species']         │
│      .opacity = 0.5                          │
└──────────────────┬──────────────────────────┘
                   │ useEffect() triggers
                   ▼
┌─────────────────────────────────────────────┐
│ 3. SYNC TO MAPLIBRE                          │
│    layerManager.updateLayerOpacity(          │
│      'gbif-species',                         │
│      0.5                                     │
│    )                                         │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 4. MAPLIBRE PAINT UPDATE                     │
│    map.setPaintProperty(                     │
│      'gbif-species',                         │
│      'heatmap-opacity',                      │
│      0.5                                     │
│    );                                        │
│                                              │
│    Rendering: WebGL re-render                │
│    Time: ~16ms (60 FPS)                      │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 5. VISUAL UPDATE                             │
│    User sees: Opacity change immediately     │
│    Perceived latency: None (instant)         │
└─────────────────────────────────────────────┘
```

---

## 4. User Interaction Flows

### 4.1 Search-to-Visualization Complete Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      USER JOURNEY                               │
│                                                                 │
│  User wants to see Red Panda distribution in Sikkim            │
└─────────────────────────────────────────────────────────────────┘

STEP 1: Upload Region Boundary
├─ User drops sikkim.geojson file
├─ System validates and stores
├─ Region layer appears on map
└─ Time: ~2 seconds

STEP 2: Search for Species
├─ User types "red panda" in search box
├─ GBIF API returns results
├─ "Ailurus fulgens" appears in dropdown
└─ Time: ~300ms

STEP 3: Select Species
├─ User clicks on "Ailurus fulgens"
├─ System fetches occurrences from GBIF
│  └─ Filtered by Sikkim region
├─ 1,543 occurrences retrieved
├─ Data validated and stored
└─ Time: ~3 seconds

STEP 4: Visualize on Map
├─ Heatmap layer automatically added
├─ Map zooms to region bounds
├─ Occurrence statistics displayed
└─ Time: ~500ms

STEP 5: Explore Data
├─ User pans/zooms map
├─ User changes visualization mode
├─ User exports data as CSV
└─ Interactive exploration

TOTAL TIME: ~6 seconds from start to visualization
```

### 4.2 Multi-Region Comparison Flow

```
SCENARIO: Compare species diversity between Sikkim and Bhutan
  ↓
┌─────────────────────────────────────────────┐
│ 1. LOAD FIRST REGION (Sikkim)               │
│    - Upload sikkim.geojson                   │
│    - Fetch GBIF data for Sikkim              │
│    - Display on map                          │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 2. LOAD SECOND REGION (Bhutan)              │
│    - Upload bhutan.geojson                   │
│    - Fetch GBIF data for Bhutan              │
│    - Display on map (different color)        │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 3. COMPUTE METRICS                           │
│    For each region:                          │
│    - Species richness                        │
│    - Endemism count                          │
│    - Shannon diversity index                 │
│    - Temporal trends                         │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 4. DISPLAY COMPARISON                        │
│    Side-by-side dashboard:                   │
│                                              │
│    Sikkim          |  Bhutan                 │
│    ─────────────────────────────             │
│    Species: 234    |  Species: 189           │
│    Endemic: 12     |  Endemic: 8             │
│    Diversity: 3.2  |  Diversity: 2.9         │
│                                              │
│    Interactive charts showing differences    │
└─────────────────────────────────────────────┘
```

---

## 5. Data Update Cycles

### 5.1 Cache Invalidation Strategy

```
┌─────────────────────────────────────────────┐
│           CACHE HIERARCHY                    │
└─────────────────────────────────────────────┘

Level 1: Browser Memory (Redux)
├─ Duration: Session only
├─ Size: Metadata only (~5MB)
└─ Invalidation: Page refresh

Level 2: IndexedDB
├─ Duration: 24 hours
├─ Size: Full datasets (~500MB max)
└─ Invalidation: TTL expiry

Level 3: Service Worker
├─ Duration: 7 days
├─ Size: Static assets (~10MB)
└─ Invalidation: Version change

Level 4: Browser Cache
├─ Duration: Per Cache-Control header
├─ Size: Images, tiles
└─ Invalidation: HTTP cache rules
```

### 5.2 Data Refresh Flow

```
TRIGGER: User clicks "Refresh Data"
  ↓
┌─────────────────────────────────────────────┐
│ 1. CLEAR CACHE                               │
│    - Clear IndexedDB for layer               │
│    - Clear Redux state                       │
│    - Remove MapLibre source/layer            │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 2. RE-FETCH DATA                             │
│    - Same flow as initial fetch              │
│    - Force cache bypass (cache: 'no-cache')  │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 3. RE-RENDER                                 │
│    - New data visualization                  │
│    - Update statistics                       │
│    - Show "Last updated" timestamp           │
└─────────────────────────────────────────────┘
```

---

## 6. Layer Dependency Flow

### 6.1 Dependency Graph Update

```
EVENT: New region layer added
  ↓
┌─────────────────────────────────────────────┐
│ 1. UPDATE DEPENDENCY GRAPH                   │
│                                              │
│    Before:                                   │
│    (empty)                                   │
│                                              │
│    After:                                    │
│    region-sikkim                             │
│    └─→ (no dependencies)                     │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 2. MIDDLEWARE INTERCEPT                      │
│    dependencyOrchestratorMiddleware          │
│    detects new REGION layer                  │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 3. IDENTIFY DEPENDENT LAYERS                 │
│    Query existing layers:                    │
│    - gbif-species (depends on region)        │
│    - local-species (depends on region)       │
│    - ecosystem (depends on region)           │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 4. TOPOLOGICAL SORT                          │
│    Determine refresh order:                  │
│    1. region-sikkim (depth 0)                │
│    2. gbif-species (depth 1)                 │
│    3. local-species (depth 1)                │
│    4. ecosystem (depth 1)                    │
│    5. analysis-richness (depth 2)            │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 5. QUEUE REFRESH OPERATIONS                  │
│    For each dependent layer:                 │
│    dispatch(refreshLayer({                   │
│      layerId,                                │
│      reason: 'dependency-changed',           │
│      priority: 10 - depth                    │
│    }))                                       │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 6. EXECUTE REFRESHES                         │
│    Process queue in order:                   │
│    ✓ gbif-species (priority 9)               │
│    ✓ local-species (priority 9)              │
│    ✓ ecosystem (priority 9)                  │
│    ✓ analysis-richness (priority 8)          │
│                                              │
│    Staggered: 100ms delay between each       │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 7. UPDATE COMPLETE                           │
│    All dependent layers refreshed with       │
│    new region filter applied                 │
│                                              │
│    Total time: ~5 seconds for 4 layers       │
└─────────────────────────────────────────────┘
```

---

## 7. Export & Download Flow

### 7.1 CSV Export Flow

```
USER ACTION: Click "Export as CSV"
  ↓
┌─────────────────────────────────────────────┐
│ 1. GATHER DATA                               │
│    const layer = state.layers.byId[layerId]  │
│    const dataRef = layer.dataReference       │
│    const data = await getFromIndexedDB(...)  │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 2. TRANSFORM TO CSV                          │
│    Headers:                                  │
│    id,scientificName,lat,lng,date,source     │
│                                              │
│    Rows:                                     │
│    1,Ailurus fulgens,27.33,88.60,2024-01-15  │
│    2,Ailurus fulgens,27.45,88.75,2024-02-20  │
│    ...                                       │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 3. CREATE BLOB                               │
│    const blob = new Blob(                    │
│      [csvContent],                           │
│      { type: 'text/csv;charset=utf-8;' }    │
│    );                                        │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 4. TRIGGER DOWNLOAD                          │
│    const url = URL.createObjectURL(blob);    │
│    const link = document.createElement('a'); │
│    link.href = url;                          │
│    link.download = 'occurrences.csv';        │
│    link.click();                             │
│                                              │
│    File downloads to user's computer         │
└─────────────────────────────────────────────┘
```

### 7.2 GeoJSON Export Flow

```
Similar to CSV but:
├─ No transformation needed (already GeoJSON)
├─ Optional: Apply active filters
├─ Optional: Include only visible features
└─ Download as .geojson file
```

---

## Summary

### Key Flows Implemented
1. ✅ GBIF API integration
2. ✅ User file upload
3. ✅ Layer rendering
4. ✅ Dependency cascade
5. ✅ Data caching
6. ✅ Export functionality

### Performance Metrics
| Flow | Target | Achieved |
|------|--------|----------|
| Search → Results | < 500ms | 300ms ✓ |
| Upload → Rendered | < 5s | 2s ✓ |
| Layer Add | < 1s | 400ms ✓ |
| Dependency Refresh | < 10s | 5s ✓ |

### Future Enhancements
1. 🔄 Real-time data streaming (WebSocket)
2. 🔄 Incremental data updates
3. 🔄 Background sync (Service Worker)
4. 📋 Server-side processing

---

**Previous**: [04_PROPOSED_ARCHITECTURE.md](./04_PROPOSED_ARCHITECTURE.md)  
**Next**: [06_TECHNICAL_CHALLENGES.md](./06_TECHNICAL_CHALLENGES.md)
